# 高压蒸汽灭菌热穿透校核定版服务

## 项目目标

建设供灭菌工艺工程师校核高压蒸汽灭菌实验的单体服务，闭环为“探头记录接入—校准归一—暴露段识别—热穿透计算—规则裁决—证据定版”。Go 后端提供 HTTP API、SQLite 持久化、有界工作池和可恢复租约；可构建的 TypeScript 页面通过真实 API 上传记录、冻结输入、启动或取消分析、查看探头结论并下载证据。生产 Go 代码规划约 2500 有效行、28 个职责文件，限定在 2000—3000 行，分布于至少 8 个业务包；测试、前端、迁移、注释和生成内容不计入额度。

## 端到端业务流程

1. 实验接入：使用幂等键创建灭菌运行修订，登记设备、运行区间、探头角色和判定参数；分块上传温度、绝对压力及校准记录。同一键与相同规范请求返回原响应，载荷不同则整批拒绝。
2. 输入冻结：服务校验探头身份、校准有效期、时间覆盖及单位，生成排序稳定的规范快照和 SHA-256。冻结请求必须匹配 expected_version；冻结后原始记录与参数不可修改，调整内容需生成后继修订。
3. 分析执行：启动动作以事务创建唯一活动任务。工作者从冻结快照构造公共时间网格，执行插值、保守温度修正、暴露段识别、饱和蒸汽相容性检查和各探头杀灭效力积分，并按确定步骤更新整数进度。
4. 结果裁决：任务完成时原子写入暴露段、探头指标和有序发现。所有必需条件满足时生成通过候选；缺少暴露段、热穿透不足、温度均匀性或压力温度相容性不合格时生成未通过候选，调用者不能改写结论。
5. 证据定版：定版动作将规范快照哈希、算法配置、暴露段、逐探头指标、发现和逻辑状态事件编码为稳定归档，在同一事务中写入归档、哈希和终态；重复幂等请求返回完全相同的字节。
6. 重放与恢复：历史修订可从不可变快照只读重算并比较指标及证据哈希。服务启动时扫描租约；未过期任务保持等待，过期任务只能被一个实例认领并从快照完整重算，任何中间退出都不公开部分结果。

## 核心组件与职责

1. 接入 API 与实验页面：解析运行参数、探头清单、CSV 记录和校准信息，实施幂等与版本控制；Go 嵌入构建后的前端，页面完成真实上传、冻结、任务控制、状态轮询、逐探头结果和证据下载。
2. 校准与时间序列归一器：统一 UTC 纳秒、摄氏度和绝对千帕，处理重复与冲突采样、校准偏移及不确定度、覆盖缺口、固定网格和有界线性插值，并生成规范快照。
3. 暴露段与热力计算器：以冻结参数识别连续合格暴露段，按固定公式计算保守温度下的逐探头杀灭效力，使用冻结的单调蒸汽表插值压力对应饱和温度，并提取冷点、温差和偏离区间。
4. 规则裁决器：检查最短暴露时长、逐探头最低效力、探头间温差、压力温度偏差及允许持续时间，生成含边界、极值、阈值、单位和原因码的稳定排序发现，并确定唯一结论。
5. 工作流与任务执行器：实现修订状态机、乐观锁、幂等动作、有界队列、租约续期、假时钟、取消和超时；提交结果前再次比较状态版本，确保取消与完成只有一个胜者。
6. SQLite 与证据恢复模块：以事务、外键、唯一索引和不可变触发器保存快照、任务、指标、事件及归档；固定字段、文件路径、时间戳和压缩参数，支持启动恢复、完整性检查和确定性重放。

## 领域规则与不变量

1. 内部时间仅使用带 Z 或显式偏移的 RFC3339Nano，解析后保存为 UTC 整数纳秒；拒绝无时区、闰秒文本、溢出或精度损失。温度统一为摄氏度，压力只接受明确标记的绝对千帕。
2. 每个探头具有唯一标识和 chamber、load、drain 或 pressure 角色；冻结参数明确必需探头。校准记录必须覆盖完整运行区间，修正温度等于原值加偏移，保守温度再减去非负不确定度，且每项修正只能应用一次。
3. 数值必须有限。上传批次先规范化再按探头、时间及来源序号排序；同一探头同一时刻且规范值相同的记录折叠，值不同则返回 SAMPLE_CONFLICT 并回滚整个批次。上传顺序不得影响快照哈希。
4. 每个必需探头必须覆盖运行起止，邻接采样间隔不得超过冻结的 max_gap。公共网格由 run_start+n×sample_step、run_end 及插值交点的有序去重并集组成；精确端点复用原值，仅允许在相邻样本间线性插值，禁止外推。
5. 网格时刻的合格谓词为所有必需 load 探头保守温度不低于 exposure_min，且 chamber 探头温差不高于 spread_max。暴露起点是首个连续为真达到 confirm_duration 的区间首点；之后连续为假超过 grace_duration 时，暴露终止于该失效区间前一网格点。
6. 每个 load 探头在暴露段按梯形法积分 10^((T_lower-T_ref)/z)，秒数除以 60 后得到等效分钟；T_ref、z、步长及舍入规则随修订冻结。积分只按时间升序执行，不允许并行归并改变浮点顺序。
7. 冻结蒸汽表必须按压力严格递增且饱和温度单调。每个压力网格点以线性插值得到期望温度，超表范围属于输入错误；chamber 中位温度与期望值之差按固定容差形成连续偏离区间。
8. 最小条件以 measured-threshold 计算裕度，最大条件以 threshold-measured 计算裕度；裕度等于零为合格，负值才形成发现。跨零边界使用线性插值，发现按规则优先级、探头标识、开始时间和结束时间排序。
9. 通过结论要求存在合格暴露段、每个必需 load 探头效力达标、暴露时长达标、温差合格且蒸汽相容性偏离未超过允许持续时间；任一失败均产生未通过结论，不存在人工覆盖或缺省通过。
10. 主状态为 DRAFT→FROZEN→ANALYZING→RESULT_READY→SEALED_PASS 或 SEALED_FAIL。ANALYZING 可由成功取消进入 ABORTED；终态不可逆。每个写动作同时匹配修订 ID、当前状态、expected_version，并在成功后递增版本。
11. 同一修订最多存在一个活动分析或重放任务。取消和完成使用同一状态版本竞争；取消先提交后，工作者的结果提交必须失败。任务重试从冻结快照完整计算，不复用未提交的临时指标。
12. 证据编码先将负零规范为正零并拒绝 NaN 或 Inf；哈希不含数据库自增值、墙钟时间、租约持有者、进度和幂等键，只包含规范业务数据、算法版本、逻辑事件、指标、发现及结论。

## 数据模型与持久化

1. sterilization_runs：设备标识、运行业务键、当前修订号和创建顺序；业务键唯一。
2. revisions：修订号、状态、lock_version、前驱修订、冻结快照哈希、算法配置版本和终态结论。
3. probe_specs、calibrations：探头角色、是否必需、允许单位、校准有效区间、偏移和不确定度；冻结后由触发器保护。
4. sensor_samples：修订、探头、UTC 纳秒、规范值、来源分块哈希和稳定序号；探头与时刻唯一。
5. requirement_snapshots：运行边界、采样步长、最大缺口、暴露温度及时长、确认与宽限时长、温差、效力、蒸汽偏差、参考温度、z 值及蒸汽表版本。
6. idempotency_records：动作作用域、键、规范请求哈希、响应状态、响应体和目标版本；复合唯一键归并并发请求。
7. analysis_jobs：修订、任务类型、状态、租约持有者、lease_until、attempt、完成步数、总步数、取消标记和稳定错误码。
8. exposure_segments、probe_metrics：暴露起止、持续时间、冷点，以及每个探头的最低温度、最高温度、等效分钟和摘要哈希。
9. findings：稳定序号、规则码、探头或区域、区间、首次失败时刻、实测极值、阈值、裕度、单位和严重度。
10. state_events：修订内单调序号、源状态、目标状态、原因码和结果版本；墙钟时间仅供运行诊断。
11. evidence_packages：清单版本、归档 BLOB、内容长度、SHA-256、complete 标记和定版版本；归档写入、终态迁移及事件必须同事务提交。

## 公开接口

1. POST /api/v1/sterilization-runs：使用 Idempotency-Key 创建运行及草拟修订，返回 revision_id、state 和 version。
2. POST /api/v1/revisions/{id}/probes、/calibrations、/samples:batch、/requirements：携带 expected_version 和 Idempotency-Key 接入 JSON 或公开 CSV 格式；整批验证成功后提交。
3. POST /api/v1/revisions/{id}:freeze 与 :revise：冻结规范输入或从历史修订复制新的可编辑修订，均执行版本比较和幂等保护。
4. POST /api/v1/revisions/{id}:start、:cancel、:finalize：启动分析、竞争式取消或原子定版；正文包含 expected_version，响应包含新版本和稳定任务标识。
5. GET /api/v1/revisions/{id}、/job、/events、/results：返回输入完整性、整数进度、逻辑事件、暴露段、逐探头指标和有序发现；定版前不返回证据字节。
6. POST /api/v1/revisions/{id}:replay 与 GET /api/v1/revisions/{id}/replays/{task_id}：启动只读重放并返回指标、发现、结论和哈希的逐项比较。
7. GET /api/v1/revisions/{id}/evidence：仅在 SEALED_PASS 或 SEALED_FAIL 且 complete=true 时返回 application/zip、ETag 和 SHA-256。
8. GET /healthz、GET /readyz 与浏览器入口 /：就绪检查覆盖迁移、工作池和恢复扫描；页面使用锁定依赖构建且不依赖运行时 CDN，所有业务内容均来自上述 API。

## 失败边界

1. 未知探头、校准过期、单位含糊、时间重复冲突、覆盖缺口或蒸汽表非法在冻结前返回定位明确的输入错误，失败事务不得改变草稿或版本。
2. 无法识别合格暴露段、效力不足、温差超限或蒸汽相容性失败属于可定版的规则结论；数值溢出、非有限中间值或快照损坏以 DATA_UNRECOVERABLE 中止任务。
3. 工作池已满、SQLite 短暂忙碌或租约续期失败返回 COMPUTE_TEMPORARY；工作者丢失租约后不得提交指标。
4. 并发启动受活动任务唯一索引约束；过期版本、非法状态和幂等载荷冲突分别返回 VERSION_CONFLICT、ILLEGAL_STATE 和 IDEMPOTENCY_CONFLICT。
5. 取消与完成竞争只允许一个事务成功；取消胜出后不得查询到暴露段、指标、发现或证据。
6. 指标写入或定版阶段注入故障时，事务回滚全部衍生记录；重启仅认领已过期租约，并从冻结快照重新计算。
7. 已定版归档的长度、BLOB 或哈希不一致时禁止下载并报告 EVIDENCE_CORRUPT；系统不得现场重建后冒充原归档。
8. 统一错误体包含 code、category、message、retryable、revision_version 和有序 details，不在错误响应中泄露未定版证据。

## 验收标准

1. 公开夹具可完整执行接入、冻结、分析和定版，通过样本与未通过样本均产生可下载证据；相同输入和算法版本重复运行得到相同暴露段、指标、发现顺序及 SHA-256。
2. 时间、校准、重复采样、覆盖缺口、端点插值和公共网格符合书面规则；上传分块顺序、工作线程数和进程重启均不改变规范快照或计算结果。
3. 解析温度曲线的等效分钟与手工固定向量在公开容差内一致；阈值相等、跨零区间、确认时长、宽限时长和多探头同时失败均按确定规则处理。
4. 并发重复上传与双启动分别只提交一次数据和一个任务；相同幂等请求复用响应，载荷变化与过期版本返回稳定错误且不污染冻结快照。
5. 在样本写入、任务完成和证据定版事务点注入故障，不出现部分结果；假时钟精确验证取消竞争、租约续期、过期认领及恢复后等价性。
6. 状态机拒绝全部非法迁移；冻结及终态修订不可修改，后继修订不改变历史，重放只产生比较结果且不改写原结论或证据。
7. 前端通过提交的锁文件完成确定性构建，由 Go 服务实际托管，并能调用真实 API 完成上传、进度轮询、结果查看和下载；多阶段 Docker 构建可生成 linux/amd64 与 linux/arm64 镜像并使用持久卷保存 SQLite。
8. go test ./...、go build ./cmd/server 均通过；生产 Go 代码至少 20 个文件、2000 有效行，目标约 2500 行且不超过 3000 行。仓库至少提交 8 个公开 Go 测试文件、33 个确定性测试，测试、前端、注释和填充内容不计入生产额度。

## 确定性测试场景

1. ingest_normalization_test.go（至少 5 例）：验证时区等价、单位转换、重复折叠、冲突回滚、乱序分块及规范哈希稳定。
2. calibration_grid_test.go（至少 4 例）：验证校准有效期、偏移只应用一次、不确定度下界、公共网格端点、最大缺口和禁止外推。
3. exposure_segment_test.go（至少 5 例）：验证确认窗口、短暂失效宽限、持续失效截断、无暴露段及运行末端精确结束。
4. lethality_steam_test.go（至少 5 例）：以固定曲线校验梯形积分、z 值、冷点、蒸汽表端点、表间插值和越界拒绝。
5. adjudication_test.go（至少 5 例）：覆盖效力、时长、温差、蒸汽偏差、零裕度安全、跨零边界和同时失败的稳定排序。
6. workflow_concurrency_test.go（至少 4 例）：使用屏障测试重复接入、双启动、旧版本动作、非法迁移及取消完成竞争。
7. persistence_recovery_test.go（至少 3 例）：使用临时 SQLite 与假时钟验证事务中断、未过期租约不抢占、过期租约单次认领和终态触发器。
8. evidence_api_e2e_test.go（至少 4 例）：运行真实 HTTP 服务完成两类结论，验证固定归档元数据、重复定版字节一致、损坏禁止下载和重放比较。

## 组件追踪关系

1. 组件 1 对应实验接入与页面闭环，落在 cmd/server、internal/api，规划 5 个生产 Go 文件、约 350 行；由接入归一化和 HTTP 端到端测试验证。
2. 组件 2 对应校准、样本规范化和冻结，落在 internal/ingest、internal/series，规划 5 个文件、约 460 行；由校准网格、冲突回滚和哈希稳定测试验证。
3. 组件 3 对应暴露识别与热力计算，落在 internal/thermal，规划 5 个文件、约 510 行；由暴露段、解析积分和蒸汽表边界测试验证。
4. 组件 4 对应规则发现与唯一结论，落在 internal/adjudicate，规划 4 个文件、约 360 行；由裕度边界、多规则触发和稳定排序测试验证。
5. 组件 5 对应状态机、队列、租约及取消，落在 internal/workflow，规划 4 个文件、约 430 行；由并发屏障、假时钟、非法迁移和重放测试验证。
6. 组件 6 对应事务存储、恢复和证据，落在 internal/store、internal/evidence，规划 5 个文件、约 390 行；由事务故障、恢复及归档字节稳定测试验证。合计 28 个生产 Go 文件、约 2500 有效行，所有文件均服务于同一灭菌热穿透校核闭环。

## 独特性

项目以校准不确定度下的保守温度、连续暴露段识别、逐探头杀灭效力积分和压力温度热力相容性为核心，将每次实验固化为可重放、可验证字节哈希的工程证据。
